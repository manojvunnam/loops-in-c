#include <stdio.h>
int main() {
    int i,base,power;
    int result=1;
    scanf("%d",&base);
    scanf("%d",&power);
    for(i=1;i<=power;i++){
        result=result*base;
    }
    printf("result=%d",result);
    return 0;
}
