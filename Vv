#include<stdio.h>
int main(){
int n,delay;
int totaldelay=0;
int countdelay=0;
if(scanf("%d",&n)!=1)
return 0;
for(int i=0;i<n;i++){
scanf("%d",&delay);
totaldelay=totaldelay+delay;
if(delay>30){
delaycount++;
}
}
printf("totaldelay=%d\n",totaldelay);
printf("delaydelivaries=%d\n",delaycount);
return 0;
}
