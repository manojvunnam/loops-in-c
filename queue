#include <stdio.h>

int main() {
    long long num, left, right;

    scanf("%lld", &num);

    right = num % 10;
    left = num;

    while (left >= 10) {
        left = left / 10;
    }

    printf("%lld %lld", left, right);

    return 0;
}
