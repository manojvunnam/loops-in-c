#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int rev=0;
    int sum=0;
    int digit;
    while(n!=0){
         digit=n%10;
        sum=sum+digit;
rev=rev*10+digit;
n=n/10;
    }
printf("%d",sum);
    return 0;
}
